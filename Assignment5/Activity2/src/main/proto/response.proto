syntax = "proto2";

package operation;

option java_package = "buffers";
option java_outer_classname = "ResponseProtos";


// A response from the server can be any of these types 
message Response {
  enum ResponseType {
    GRANTED = 0;     // grants permission
    DENIED = 1;       // denies permission
  }
}
